name: secure_alarm
description: スマートフォン盗難防止アプリ
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: 4.0.2
  vibration: 1.9.0
  shared_preferences: 2.5.3
  provider: 6.1.5+1
  wakelock_plus: 1.2.8
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
